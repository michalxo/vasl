package VASL.build.module.fullrules.MovementClasses.HexandLocation;

public interface HexDecoratori extends Locationi {
    // Enables decorators that wrap the concrete component classes of Locationi via the hex Decorator interface
}
